export interface User {
  id: string;
  username: string;
  joinDate: string;
}
export interface Employee {
  id: number;
  username: string;
  department: string;
  city: string;
  joinDate: string;
  salary: number;
}